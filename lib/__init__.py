"""
Chess Board Library Package

This __init__.py file makes the lib/ directory a Python package.
Without it, Python would not recognize lib/ as a package and imports like
'from lib.leds import LEDController' would fail.

The file can be empty - it just needs to exist.
"""
